#
# Executes commands at the start of an interactive session.
#
# Authors:
#   Sorin Ionescu <sorin.ionescu@gmail.com>
#

# Source Prezto.
if [[ -s "${ZDOTDIR:-$HOME}/.zprezto/init.zsh" ]]; then
  source "${ZDOTDIR:-$HOME}/.zprezto/init.zsh"
fi

# Customize to your needs...

alias gitrm="git ls-files -d | xargs git rm"
alias git_pull_all='for i in `find . -name ".git" -type d -depth 2`; do r=`dirname $i`; echo ">>>>>>> Updating $r <<<<<<<<"; cd $r; git stash && git pull --rebase && git stash pop; cd -; echo "<<<<<<<<<< DONE >>>>>>>>>"; done'
unalias rm

alias -s -- log=vim

bindkey '^R' history-incremental-search-backward
bindkey '^S' history-incremental-search-forward
bindkey '^P' history-search-backward
bindkey '^N' history-search-forward

function git_upgrade_all() {
    git pull origin &&
    git submodule update --init --recursive &&
    git submodule foreach --recursive 'git fetch origin && git checkout `git rev-parse origin/HEAD`'
}

PATH=$PATH:$HOME/.rvm/bin # Add RVM to PATH for scripting
